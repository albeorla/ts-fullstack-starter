# Test environment configuration for Docker and E2E tests
# This file is used when NODE_ENV=test or when running in Docker

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Next Auth
# You can generate a new secret on the command line with:
# npx auth secret (or yarn dlx auth secret)
# https://next-auth.js.org/configuration/options#secret
AUTH_SECRET="test-secret-for-ci-do-not-use-in-production"

# Next Auth Discord Provider (Test App)
# These should be from a separate Discord app used only for testing
AUTH_DISCORD_ID="test-discord-id"
AUTH_DISCORD_SECRET="test-discord-secret"

# Prisma
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
# Note: 'localhost' for local development, 'db' hostname is used for Docker container networking in CI
DATABASE_URL="postgresql://postgres:password@localhost:5432/test_db?schema=public"

# Test Authentication (for Playwright E2E tests)
# Only enable this for testing environments
ENABLE_TEST_AUTH="true"
VERBOSE_TEST_LOGS="true"
TEST_USER_EMAIL="test@example.com"
TEST_USER_PASSWORD="test-password-123"

# Build Configuration
# Skip environment validation during Docker builds if needed
# SKIP_ENV_VALIDATION="true"

# PostgreSQL container settings
POSTGRES_PASSWORD=password
POSTGRES_DB=test_db
